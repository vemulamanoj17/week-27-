## manual install
 -install nodejs 
 -clone the repo
 -install dependendcies
 -start db locally
    -- docker run 
    --postgres_password=mysecretpassword -d -p 5432
# docker installation
# docker-compose installation